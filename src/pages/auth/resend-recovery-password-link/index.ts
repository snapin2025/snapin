import { Metadata } from 'next'

export { ResendRecoveryPasswordLink } from './ResendRecoveryPasswordLink'

export const metadata: Metadata = {
  title: 'Recovery password',
  description: 'Resend link to recovery password'
}
